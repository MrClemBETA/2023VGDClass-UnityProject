using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LateefManager : MonoBehaviour
{
    [SerializeField] float lateefAM = 0.7f;
    [SerializeField] int lateefBA = 0;
    [SerializeField] float lateefDD = 1.2f;
    [SerializeField] int lateefBD = 3;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
